# Story 4-4 手工测试验收清单

## 前置条件

- [ ] 应用正常启动，显示主界面（Sidebar + Workspace）
- [ ] 已完成 onboarding（API Key 已配置）
- [ ] 至少存在一个会话

---

## AC #1: Dock Badge 未读会话计数（FR47）

**目标：** 验证应用不在前台时，Agent 完成后 Dock 图标显示未读数量 badge

- [ ] **1.1 Agent 完成后显示 badge**
  - 步骤：发送消息触发 Agent 执行 → 在 Agent 执行期间 Cmd+Tab 切换到其他应用 → 等待 Agent 完成
  - 预期：Dock 图标上显示红色椭圆 badge "1"

- [ ] **1.2 多会话未读累计**
  - 步骤：创建两个会话 → 分别在两个会话中触发 Agent 执行（每次执行时切到其他应用）
  - 预期：Dock badge 显示 "2"

- [ ] **1.3 点击 Dock 图标回前台清除 badge**
  - 步骤：badge 显示时点击 Dock 图标回到 SwiftWork
  - 预期：badge 立即消失

- [ ] **1.4 选中未读会话清除对应计数**
  - 步骤：有 2 个未读会话 → 点击其中一个未读会话
  - 预期：badge 从 "2" 变为 "1"

- [ ] **1.5 应用在前台时不显示 badge**
  - 步骤：保持 SwiftWork 在前台 → 触发 Agent 执行并等待完成
  - 预期：Dock 图标始终无 badge

- [ ] **1.6 删除未读会话清除计数**
  - 步骤：有未读会话 → 右键删除该会话
  - 预期：badge 数字减 1（或消失如果是唯一未读）

---

## AC #2: 窗口位置和大小恢复（NFR18, NFR21）

**目标：** 验证关闭并重新打开应用后，窗口恢复到上次的位置和大小

- [ ] **2.1 窗口位置恢复**
  - 步骤：拖动窗口到屏幕右下角 → Cmd+Q 退出 → 重新启动应用
  - 预期：窗口出现在屏幕右下角（上次关闭位置）

- [ ] **2.2 窗口大小恢复**
  - 步骤：调整窗口到非默认大小（如拉宽） → Cmd+Q 退出 → 重新启动应用
  - 预期：窗口大小与关闭时一致

- [ ] **2.3 移动后自动保存**
  - 步骤：拖动窗口到新位置（等待 500ms 节流） → 不退出，直接观察
  - 预期：通过后台日志或重新打开验证位置已保存

---

## AC #3: 全屏/分屏/Stage Manager 兼容性（NFR18）

**目标：** 验证应用在不同窗口模式下 UI 正确适配

- [ ] **3.1 全屏模式**
  - 步骤：点击窗口绿色按钮进入全屏
  - 预期：NavigationSplitView 三栏布局正确扩展，无变形或溢出

- [ ] **3.2 退出全屏**
  - 步骤：按 Esc 退出全屏
  - 预期：恢复到之前的窗口大小，布局正常

- [ ] **3.3 分屏模式**
  - 步骤：长按绿色按钮 → 选择分屏（左/右）
  - 预期：窗口自动调整为半屏大小，三栏布局正确重排

- [ ] **3.4 Stage Manager**
  - 步骤：启用 Stage Manager（系统设置 → Stage Manager）→ 观察窗口缩略图
  - 预期：SwiftWork 窗口缩略图正确显示，点击恢复后布局正常

---

## 回归验证

- [ ] **4.1 菜单栏快捷键**
  - 步骤：测试 Cmd+N / Cmd+I / Cmd+Shift+D / Cmd+,
  - 预期：所有快捷键功能正常（Story 4-3 无回归）

- [ ] **4.2 Sidebar 会话管理**
  - 步骤：创建/选择/删除会话
  - 预期：功能与之前版本一致

- [ ] **4.3 Agent 执行**
  - 步骤：发送消息触发 Agent 执行
  - 预期：Timeline 正常显示事件流

- [ ] **4.4 设置页面**
  - 步骤：Cmd+, 打开设置 → 验证各 Tab 功能
  - 预期：设置页面功能正常

---

## 自动化测试结果

| 指标 | 结果 |
|------|------|
| 总测试数 | 765 |
| 失败数 | 0 |
| 新增 Story 4-4 测试 | 23 (DockBadgeTests 12 + WindowStateTests 11) |
| 覆盖率 | 100% |
| 质量门 | PASS |

---

## 验收决策

- [ ] 所有 AC 测试项通过 → **Approve**
- [ ] 存在阻塞性问题 → **Rework**（记录问题详情）
- [ ] 非阻塞性问题 → **Approve with notes**

验收人：__________  日期：__________  决策：__________
